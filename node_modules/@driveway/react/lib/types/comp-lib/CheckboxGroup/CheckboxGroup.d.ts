import React from "react";
export declare type CheckboxOption = {
    label: string;
    checked: boolean;
    count?: number;
    image?: JSX.Element;
};
export declare type CheckboxGroupProps = {
    variant: string;
    options: CheckboxOption[];
    onChange: (event: React.ChangeEvent<HTMLInputElement>) => void;
    groupLabel?: string;
    hintText?: string;
    error?: boolean;
};
declare const CheckboxGroup: ({ variant, options, onChange, error, groupLabel, hintText, }: CheckboxGroupProps) => JSX.Element;
export default CheckboxGroup;
