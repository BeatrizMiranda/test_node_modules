import { HTMLAttributes, ReactNode, SyntheticEvent } from "react";
export declare type AlertProps = HTMLAttributes<HTMLDivElement> & {
    id?: string;
    variant: "error" | "success" | "warning" | "info";
    message: ReactNode;
    closeAlert?: (e: SyntheticEvent) => void;
    onClick?: () => void;
    isAnchored?: boolean;
    enableCloseIcon?: boolean;
};
declare const Alert: ({ id, variant, message, closeAlert, onClick, isAnchored, enableCloseIcon, ...props }: AlertProps) => JSX.Element | null;
export default Alert;
