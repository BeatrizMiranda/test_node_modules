import { TabsProps as MUITabsProps } from "@mui/material/Tabs";
import { ChangeEvent } from "react";
declare type TabsProps = Omit<MUITabsProps, "onChange"> & {
    onChange?: (event: ChangeEvent<{}>, value: any) => void;
    size?: "small" | "medium" | "large";
};
declare const Tabs: ({ children, classes, className, size, ...rest }: TabsProps) => JSX.Element;
export default Tabs;
