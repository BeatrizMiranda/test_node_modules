/// <reference types="react" />
import { AnimationConfigWithData, AnimationDirection, AnimationEventCallback, AnimationEventName, AnimationSegment } from "lottie-web";
export declare type AnimationOptions = Pick<AnimationConfigWithData, "animationData" | "autoplay" | "loop" | "renderer" | "rendererSettings">;
export declare type AnimationEventListener = {
    eventName: AnimationEventName;
    callback: AnimationEventCallback;
};
export declare type LottieProps = {
    ariaLabel?: string;
    ariaRole?: string;
    className?: string;
    direction?: AnimationDirection;
    eventListeners?: AnimationEventListener[];
    height?: number | string;
    isClickToPauseDisabled?: boolean;
    isPaused?: boolean;
    isStopped?: boolean;
    options: AnimationOptions;
    segments?: AnimationSegment[];
    speed?: number;
    tabIndex?: number;
    title?: string;
    width?: number | string;
};
/**
 * A React component for displaying [Lottie](https://github.com/airbnb/lottie-web) animations.
 *
 * ## Usage
 * To display a Lottie animation simply proide the animation data JSON,
 * either via options.animationData (e.g. via import).
 *
 * ### Example: Using options.animationData (Recommended method)
 * ```typescript
 * import animationData from "path/to/animation.json";
 *
 * <Lottie options={{animationData}} />
 * ```
 *
 * Please refer to the [lottie-web](https://github.com/airbnb/lottie-web)
 * library docs for more info on functionality and customization
 * (e.g. [event listeners](https://github.com/airbnb/lottie-web#events)).
 */
declare const Lottie: ({ ariaLabel, ariaRole, className, direction, eventListeners, height, isClickToPauseDisabled, isStopped, isPaused, options: animationConfig, segments, speed, tabIndex, title, width, }: LottieProps) => JSX.Element;
export default Lottie;
