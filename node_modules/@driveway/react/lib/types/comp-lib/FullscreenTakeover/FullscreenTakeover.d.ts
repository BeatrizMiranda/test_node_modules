import { DialogProps } from "@mui/material";
import { ReactNode } from "react";
export declare const useTransitionDuration: () => number;
export declare type FullScreenTakeOverProps = DialogProps & {
    headerTitle: string;
    headerImg?: string;
    backButtonTitle: string;
    onBackButtonClick?: () => void;
    open: boolean;
    stepHeader?: ReactNode;
};
declare const FullscreenTakeover: ({ headerTitle, headerImg, backButtonTitle, onBackButtonClick, children, open, stepHeader, ...props }: FullScreenTakeOverProps) => JSX.Element;
export default FullscreenTakeover;
