import { ReactNode } from "react";
export declare type TabPanelProps = {
    activeTab: number;
    index: any;
    children: ReactNode;
};
declare const TabPanel: ({ children, index, activeTab, ...rest }: TabPanelProps) => JSX.Element;
export default TabPanel;
