import { HTMLAttributes, ReactNode, SyntheticEvent } from "react";
export declare type BannerProps = HTMLAttributes<HTMLDivElement> & {
    id?: string;
    variant: "error" | "success" | "attention" | "neutral" | "info";
    message: ReactNode;
    closeBanner?: (e: SyntheticEvent) => void;
    onClick?: () => void;
    isInline?: boolean;
    disableCloseIcon?: boolean;
    disableStatusIcon?: boolean;
    justifyContentCenter?: boolean;
};
/**
   *
   ## Banner Usage
    The **Banner** component can be used to display success or
    failure information either above the main header of Driveway.com, i.e.,
    the header in the **Layout** component, or inline in a component.
  **/
declare const Banner: ({ id, variant, message, closeBanner, onClick, isInline, disableCloseIcon, disableStatusIcon, justifyContentCenter, ...props }: BannerProps) => JSX.Element | null;
export default Banner;
