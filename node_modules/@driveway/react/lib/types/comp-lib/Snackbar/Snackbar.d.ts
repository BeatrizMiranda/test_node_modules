import { ButtonProps } from "comp-lib/Button";
import { Dispatch, ReactNode, SetStateAction } from "react";
declare type SnackbarProps = {
    isOpen: boolean;
    setIsOpen: Dispatch<SetStateAction<boolean>>;
    message: ReactNode;
    actionBtnProps?: ButtonProps;
    onScreenDuration?: number;
    transitionDuration?: number;
    travelDirection?: "left" | "right" | "up" | "down";
    anchorOrigin?: {
        horizontal: "center" | "left" | "right";
        vertical: "bottom" | "top";
    };
};
declare const Snackbar: ({ isOpen, setIsOpen, message, actionBtnProps, onScreenDuration, transitionDuration, travelDirection, anchorOrigin, }: SnackbarProps) => JSX.Element;
export default Snackbar;
export type { SnackbarProps };
