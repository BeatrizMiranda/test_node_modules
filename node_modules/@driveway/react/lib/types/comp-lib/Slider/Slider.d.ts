import { SliderProps as MuiSliderProps } from "@mui/material";
import React from "react";
declare type C = React.ElementType;
declare type SliderProps = Omit<MuiSliderProps<C, {
    component?: C;
}>, "onChange"> & {
    onChange: (arg0: any, arg1: any) => void;
    subHeader?: string;
    minMaxLabels?: number[];
};
declare const Slider: ({ subHeader, minMaxLabels, className, ...sliderProps }: SliderProps) => JSX.Element;
export default Slider;
