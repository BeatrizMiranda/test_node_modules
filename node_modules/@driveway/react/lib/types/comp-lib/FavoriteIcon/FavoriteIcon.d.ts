/// <reference types="react" />
import type { ButtonProps } from "comp-lib/Button";
declare type FavoriteIconProps = Omit<ButtonProps, "variant" | "borderless" | "size" | "bare"> & {
    saved: boolean;
    width?: number;
    height?: number;
    count?: number;
    sprite: string;
    isLoading?: boolean;
    onClick: () => void;
};
declare const FavoriteIcon: ({ saved, isLoading, count, width, height, sprite, onClick, className, ...props }: FavoriteIconProps) => JSX.Element;
export default FavoriteIcon;
export type { FavoriteIconProps };
