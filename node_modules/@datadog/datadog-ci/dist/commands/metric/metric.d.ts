import { Command } from 'clipanion';
export declare const parseMetrics: (metrics: string[]) => {};
export declare class MetricCommand extends Command {
    static paths: string[][];
    static usage: import("clipanion").Usage;
    private level;
    private metrics;
    private noFail;
    private config;
    execute(): Promise<number>;
    private sendMetrics;
}
