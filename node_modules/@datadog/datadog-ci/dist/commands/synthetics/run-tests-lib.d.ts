import { APIHelper } from './api';
import { MainReporter, Reporter, Result, RunTestsCommandConfig, Suite, Summary, SupportedReporter, UserConfigOverride, WrapperConfig } from './interfaces';
type ExecuteOptions = {
    jUnitReport?: string;
    reporters?: (SupportedReporter | Reporter)[];
    runId?: string;
    suites?: Suite[];
};
export declare const executeTests: (reporter: MainReporter, config: RunTestsCommandConfig, suites?: Suite[]) => Promise<{
    results: Result[];
    summary: Summary;
}>;
export declare const getTestsList: (api: APIHelper, config: RunTestsCommandConfig, reporter: MainReporter, suites?: Suite[]) => Promise<{
    config: UserConfigOverride;
    id: string;
    suite: string | undefined;
}[]>;
export declare const executeWithDetails: (runConfig: WrapperConfig, { jUnitReport, reporters, runId, suites }: ExecuteOptions) => Promise<{
    results: Result[];
    summary: Summary;
    exitCode: 0 | 1;
}>;
export declare const execute: (runConfig: WrapperConfig, executeOptions: ExecuteOptions) => Promise<0 | 1>;
export {};
