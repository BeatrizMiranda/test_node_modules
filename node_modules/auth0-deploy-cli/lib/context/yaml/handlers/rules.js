"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _path = require("path");

var _path2 = _interopRequireDefault(_path);

var _fsExtra = require("fs-extra");

var _fsExtra2 = _interopRequireDefault(_fsExtra);

var _utils = require("../../../utils");

var _logger = require("../../../logger");

var _logger2 = _interopRequireDefault(_logger);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

async function parse(context) {
  if (!context.assets.rules) return {};
  return {
    rules: [...context.assets.rules.map(rule => ({ ...rule,
      script: context.loadFile(rule.script)
    }))]
  };
}

async function dump(context) {
  let rules = [...(context.assets.rules || [])];

  if (rules.length > 0) {
    const rulesFolder = _path2.default.join(context.basePath, 'rules');

    _fsExtra2.default.ensureDirSync(rulesFolder);

    rules = rules.map(rule => {
      const scriptName = (0, _utils.sanitize)(`${rule.name}.js`);

      const scriptFile = _path2.default.join(rulesFolder, scriptName);

      _logger2.default.info(`Writing ${scriptFile}`);

      _fsExtra2.default.writeFileSync(scriptFile, rule.script);

      return { ...rule,
        script: `./rules/${scriptName}`
      };
    });
  }

  return {
    rules
  };
}

exports.default = {
  parse,
  dump
};