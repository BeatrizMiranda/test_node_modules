"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _fsExtra = require("fs-extra");

var _fsExtra2 = _interopRequireDefault(_fsExtra);

var _path = require("path");

var _path2 = _interopRequireDefault(_path);

var _tools = require("../../../tools");

var _logger = require("../../../logger");

var _logger2 = _interopRequireDefault(_logger);

var _utils = require("../../../utils");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function parse(context) {
  const clientsFolder = _path2.default.join(context.filePath, _tools.constants.CLIENTS_DIRECTORY);

  if (!(0, _utils.existsMustBeDir)(clientsFolder)) return {
    clients: undefined
  };
  const foundFiles = (0, _utils.getFiles)(clientsFolder, ['.json']);
  const clients = foundFiles.map(f => {
    const client = (0, _utils.loadJSON)(f, context.mappings);

    if (client.custom_login_page) {
      const htmlFileName = _path2.default.join(clientsFolder, client.custom_login_page);

      if ((0, _utils.isFile)(htmlFileName)) {
        client.custom_login_page = (0, _tools.loadFile)(htmlFileName, context.mappings);
      }
    }

    return client;
  }).filter(p => Object.keys(p).length > 0);
  return {
    clients
  };
}

async function dump(context) {
  const {
    clients
  } = context.assets;
  if (!clients) return;

  const clientsFolder = _path2.default.join(context.filePath, _tools.constants.CLIENTS_DIRECTORY);

  _fsExtra2.default.ensureDirSync(clientsFolder);

  clients.forEach(client => {
    const clientName = (0, _utils.sanitize)(client.name);

    const clientFile = _path2.default.join(clientsFolder, `${clientName}.json`);

    if (client.custom_login_page) {
      const html = client.custom_login_page;

      const customLoginHtml = _path2.default.join(clientsFolder, `${clientName}_custom_login_page.html`);

      _logger2.default.info(`Writing ${customLoginHtml}`);

      _fsExtra2.default.writeFileSync(customLoginHtml, html);

      client.custom_login_page = `./${clientName}_custom_login_page.html`;
    }

    (0, _utils.dumpJSON)(clientFile, (0, _utils.clearClientArrays)(client));
  });
}

exports.default = {
  parse,
  dump
};