"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _path = require("path");

var _path2 = _interopRequireDefault(_path);

var _utils = require("../../../utils");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function parse(context) {
  const baseFolder = _path2.default.join(context.filePath);

  if (!(0, _utils.existsMustBeDir)(baseFolder)) return {};

  const migrationsFile = _path2.default.join(baseFolder, 'migrations.json');

  if ((0, _utils.isFile)(migrationsFile)) {
    const migrations = (0, _utils.loadJSON)(migrationsFile, context.mappings);
    return {
      migrations
    };
  }

  return {};
}

async function dump(context) {
  const {
    migrations
  } = context.assets;
  if (!migrations || Object.keys(migrations).length === 0) return;

  const migrationsFile = _path2.default.join(context.filePath, 'migrations.json');

  (0, _utils.dumpJSON)(migrationsFile, migrations);
}

exports.default = {
  parse,
  dump
};